# BMAD loop
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml

# Dependencies & build output
node_modules
dist
dist-ssr

# Secrets — HA token / URL live in .env.local, never committed (AD-8, T0.5).
# .env.example IS tracked (keys only, no values).
.env.local
.env.*.local
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?
